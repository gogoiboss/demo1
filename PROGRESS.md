# Progress Log

- **2026-09-11 (Overprediction Framing & Limitations Disclosure Session)**:
  - **TASK 1 — Q46 Sharpened:** Replaced Q46 in `docs/judge_prep/02_JUDGE_QA.md` with the full 45% bucket-scale framing (670 of 1,500 test rows), explaining over-reliance on `prior_leg_delay` as a static value, mitigation via calibrated bounds (Pinball Loss 7.23 vs 8.12 average), and the prioritized fix (explicit in-journey recovery rate feature). Kept Q8, Q11, and Q16 untouched.
  - **TASK 2 — Formally Disclosed in `docs/LIMITATIONS.md`:** Added Section 9 disclosing the systematic overprediction on recovering trains (~25 min predicted vs ~2 min actual across 45% of test set), the partial compensation by P10-P90 conformal bounds, and the future recovery-rate feature roadmap item.
  - **TASK 3 — Cross-Check & Verification:** Confirmed consistency across all documents (`docs/RESULTS.md`, `docs/judge_prep/02_JUDGE_QA.md`, `docs/LIMITATIONS.md`). Re-ran full test suite (`pytest -v`): 68/68 passed.

- **2026-09-11 (Evaluation Reconcile & Judge Q&A Session)**:
  - **TASK 1 — Inverted Delay-Magnitude Diagnosis:** Diagnosed counterintuitive 0–15 min MAE (26.78 min) vs 15–60 min MAE (13.63 min). Confirmed empirical cause: regression to the mean in XGBoost (predictions concentrate around 25–38 min near the ~30 min dataset mean). For near-punctual trains (mean actual 2.09 min), predicting ~25 min yields ~23–26 min MAE; for 15–60 min trains (mean actual 36.57 min), predicting ~30 min lands near the actual delay center, yielding a deceptively low 13.63 min MAE. Added a disclosure note to `docs/RESULTS.md`.
  - **TASK 2 — Missing Short-Range Forecast Horizon Bucket:** Confirmed dataset sparsity — all journeys in the dataset have scheduled durations between 5.0 and 48.0 hours; N=0 for `<4h` is expected data sparsity, not a bug. Added an explanatory note to `docs/RESULTS.md`.
  - **TASK 3 — Scalability Benchmark Unit Clarified:** Confirmed exact unit measured: **3,000 journey predictions processed through the full ML pipeline** (XGBoost + MAPIE P10/P50/P90 + SHAP) at 3.30 ms per prediction with 0 failures on a single CPU node. Updated `docs/RESULTS.md` and `README.md`.
  - **TASK 4 — Pitch-Safe Summary Updated:** Updated the pitch-safe summary paragraph across `docs/RESULTS.md`, `README.md`, and `PROGRESS.md` incorporating the new 1,500-row test split metrics, per-train regression near-tie on point MAE, 55.5% Pinball Loss reduction, 89.7–91.0% Mondrian coverage, and 3.30 ms pipeline throughput.
  - **TASK 5 — Judge Q&A Documented:** Created/updated `docs/judge_prep/02_JUDGE_QA.md` with empirical evidence for Q8 (Mondrian coverage per bucket), Q11 (baseline comparison reframe around Pinball Loss), Q16 (precise throughput scalability), and added new Q46 (honest reflection on baseline comparison surprise and value of calibrated prediction intervals).
  - **Test Suite Verification:** All 68 unit tests passing (`pytest -v`).

- **2026-09-11 (Real-Data Execution Session)**:
  - **TASK 0 — Dataset Confirmation & Ingestion:** Confirmed `data/raw/train_delay.csv` is present (10,000 rows × 11 columns). Columns match all requirements (`train_number`, `journey_date`, `actual_delay_minutes`, `scheduled_travel_hours`, `distance_km`, `zone_congestion_index`, `monsoon_flag`, `fog_risk`, `coach_count`, `loco_age_years`, `delayed_gt_15min`). Ran ingestion (`python -m src.ingestion.load_kaggle --csv data/raw/train_delay.csv`) — generated `data/processed/kaggle_competition_cleaned.parquet` (0.6 MB) cleanly.
  - **TASK 1 — Real-Data Evaluations Executed:**
    - **4-Model Baseline Comparison (`eval/baseline_comparison.py`):**
      - Scheduled ETA (Zero Delay): MAE 31.26 min, Pinball loss 15.63 (-14.3% vs prior-leg)
      - Prior-Leg Baseline (Naive): MAE 36.45 min, Pinball loss 18.23
      - Per-Train Regression (No Network Features): MAE 28.15 min, Pinball loss 14.08 (-22.8% vs prior-leg)
      - RippleETA (XGBoost + MAPIE): MAE 28.22 min, Pinball loss 8.12 (-22.6% MAE improvement, 55.5% Pinball Loss reduction)
    - **Segmented Evaluation (`eval/baseline_comparison.py` & `eval/comprehensive_evaluation.py`):**
      - By Delay Magnitude: 0–15 min (N=670): RippleETA MAE 26.78m (pinball 7.23); 15–60 min (N=520): RippleETA MAE 13.63m (pinball 5.01); 60+ min (N=310): RippleETA MAE 55.79m (pinball 15.26).
      - By Horizon: Medium 4–12h (N=245): RippleETA MAE 28.16m; Long-range >12h (N=1255): RippleETA MAE 28.23m.
    - **Mondrian Conformal Coverage (`eval/comprehensive_evaluation.py`):**
      - 0–15 min bucket (N=667): 90.4% coverage (width 84.9m)
      - 15–60 min bucket (N=522): 90.6% coverage (width 81.7m)
      - 60+ min bucket (N=311): 90.4% coverage (width 82.6m)
    - **Backtest Harness Replay (`eval/backtest_harness.py` & `test_backtest_harness.py`):**
      - Executed 2025-12-31 replay: MAE P50 = 25.9 min, Pinball loss = 6.81, Coverage 90% = 100.0%. Test passed cleanly (no longer skipped).
    - **Scalability Benchmark (`jobs/scalability_benchmark.py`):**
      - Processed 3,000 trains in 9.890s (3.30 ms/train latency, 3000/3000 success count, 0 failures).
  - **TASK 2 — Documentation Updated:** Updated `docs/RESULTS.md` with 4-model baseline table, segmented evaluation tables, and Mondrian conformal coverage results. Updated `ROUND2_READINESS.md` items D, E, F, and R from blocked to completed `[x]`.
  - **TASK 3 — Light Cleanup & Test Suite Verification:**
    - Confirmed no stale file availability claims remain in documentation.
    - Executed full test suite (`pytest -v`): **68 passed, 0 skipped**.
    - Audited test assert counts across all 18 test files: 198 total assert statements for 68 test functions (healthy coverage, zero fake tests).

- **2026-09-04**: Project scaffolding complete.
- **2026-09-04**: Data ingestion layer built. Kaggle loader, data.gov.in timetable ingestion, and RailRadar API client stub with rate limiting are in place. Exploration notebook created. Selected backtesting routes: 12301/12302 Howrah Rajdhani, 12951/12952 Mumbai Rajdhani, 12625/12626 Kerala Express.

- **2026-09-05**: Baseline vs XGBoost models built and evaluated using TimeSeriesSplit. 
  - Naive Baseline MAE: 35.28 minutes
  - XGBoost CV MAE: 27.99 minutes

- **2026-09-05**: Timed Event Graph (C3 — Network Conflict Detector) built and tested.
  - Implemented `src/graph/timed_event_graph.py`: Goverde (2010) max-plus algebra, single forward traversal (no simulation loop).
  - Two edge types: running-time edges (within-train) + conflict edges (cross-train, station-pair approximation).
  - `detect_conflicts()`: accepts live-position DataFrames or node-delay mappings; baseline vs full propagation diff gives honest conflict attribution.
  - `src/graph/worked_example.py`: two scenarios — Scenario A (PPT exact inputs, no conflict fires) and Scenario B (corrected, +9 min conflict reproduced exactly).
  - **PITCH UPDATE REQUIRED**: The original +55/+40 inputs activate no conflict. With Train 56789 at +15 min, the graph reproduces the +9 min conflict, but computes +64 min total at Allahabad, not the PPT's +57 min or 15:20–16:10 window.
  - 9/9 graph tests passing (`tests/test_graph.py`).
  - Notebook `notebooks/03_conflict_graph_demo.ipynb`: valid JSON cell metadata, NetworkX visualisation, step-by-step propagation trace, and Q&A prep notes.

- **2026-09-05**: Calibrated Output Engine (C4) implemented and empirically validated.
  - MAPIE `SplitConformalRegressor` produces P10/P50/P90 delay intervals using a chronological 70/15/15 train/calibration/test split.
  - Held-out validation: **90.3% P10-P90 coverage** on 1,500 test rows, 22.53-minute p50 MAE, and 70.9-minute average interval width. This is close to the 90% target and is measured rather than assumed.
  - `AnomalyGate` uses rolling historical uncertainty variance and suspends output when current variance exceeds 3x baseline: `PREDICTION SUSPENDED — anomalous conditions`.
  - `CalibratedPredictionPipeline` combines feature engineering, optional timed-event conflict adjustment, MAPIE calibration, and anomaly-aware output.
  - Notebook `notebooks/02_baseline_vs_xgboost.ipynb` now reports the held-out coverage result.

- **2026-09-05**: FastAPI API layer (C5) implemented and tested.
  - Core calibrated prediction and health endpoints are available under `src/api/app.py`.
  - Five distinct stakeholder framings are exposed: passenger, station master, crew controller, feeder transport, and maintenance.
  - Pydantic response schemas, clear 404/422/503 handling, OpenAPI docs, and deterministic endpoint tests are included.
  - `/docs` renders successfully; full project suite passes with 21 tests.

- **2026-09-05**: Stakeholder dashboards (C6) implemented and browser-validated.
  - `dashboard/index.html`, `styles.css`, and `app.js` provide distinct Passenger, Station Controller, and Control Room views.
  - Dark control-panel design uses amber for calibrated/conflict signals and teal-gray for standard operations.
  - Live API data loads for train 20507; the Control Room replay trace is separate from the model output and shows the corrected +55 -> +64 minute graph scenario.
  - Desktop and 390px mobile browser checks passed. Setup is documented in `docs/dashboard_notes.md`.

- **2026-09-05**: Final selected-route backtest completed and documented.
  - Six selected routes, 174 chronological held-out test rows: prior-leg baseline MAE **34.746 min**, evaluated XGBoost + MAPIE P50 MAE **28.386 min**, absolute improvement **6.360 min / 18.30%**.
  - P10-P90 empirical coverage: **97.70%**, with average interval width **106.589 min**.
  - Graph conflict adjustment rows: **0** because the journey-level artifact has no station-pair live state; this is explicitly not a measured network-propagation result.
  - Real held-out Train 12301 example: actual **85.499 min**, P50 **14.200 min**, interval **0.000-84.400 min**, missed by **1.099 min**. The illustrative 12301/56789 conflict remains unvalidated on real paired data.
  - Final report: `notebooks/04_final_backtest_report.ipynb`; measured pitch numbers: `docs/RESULTS.md`.

- **2026-09-05**: Final polish and submission preparation complete.
  - README rewritten with the actual architecture, setup path, measured results, and limitations.
  - Live demo script added with Passenger, Station Controller, Control Room, API, and fallback walkthroughs.
  - MIT license and final ignore rules added; tracked-history audit found no oversized or sensitive artifacts.
  - Final claim audit distinguishes measured route results from literature ranges and the illustrative, unvalidated station-pair example.

- **2026-09-06**: End-to-end integration verified for the available public journey pipeline.
  - Added `src/pipeline.py` as the single raw CSV -> feature engineering -> XGBoost -> explicit graph boundary -> MAPIE calibration -> result orchestration path.
  - `/predict/{train_id}` now runs through that orchestration and returns stage provenance; after leakage removal, the real `20507` result is lower bound **0.0**, point estimate **35.5**, upper bound **88.8** minutes.
  - The graph stage reports `not_activated_no_station_event_state` for the journey artifact. No conflict adjustment is fabricated; paired station-event data remains required for live graph activation.
  - Removed dashboard sample predictions. With the API running, browser validation showed `API ONLINE` and the real `20507` result; with the API stopped, the dashboard shows `API UNAVAILABLE`.
  - Added `tests/test_pipeline_e2e.py`; full suite: **23 passed**.

- **2026-09-06**: Hackathon-filtered MLOps credibility layer added.
  - `config.yaml` now owns data paths and validation thresholds; input schema, missingness, and range checks run before feature engineering.
  - Pipeline uses Python logging and returns provenance: Git commit, dataset SHA-256, model artifact SHA-256, and config path.
  - Added `docs/LIMITATIONS.md`, explicitly connecting the anomaly gate to lightweight output monitoring and listing production-only future work.
  - Added validation regression tests; full suite: **25 passed**.

- **2026-09-06**: Research-to-code reconciliation fixes applied.
  - Calibration now fits only on the chronological training slice; it no longer reuses an all-data model artifact during held-out calibration.
  - Graph-enabled pipeline calls `detect_conflicts()` and applies train-specific adjustments; the default API still reports graph inactive because no station-event state is available.
  - Corrected notebooks 01-03: continuous delay target, unique-train counting, rake-proxy wording, like-for-like held-out comparison, and original graph arithmetic.
  - Dashboard and pitch sources now label the graph as a replay and conformal outputs as interval bounds; passenger trend is unknown without temporal observations.
  - Full test suite after reconciliation: **26 passed**.

- **2026-09-07**: Final audit verifications.
  - **Leakage Audit**: Verified that `train_and_calibrate()` and `backtest.py` strictly build and fit fresh models on the chronological training split. The all-data `xgboost_delay_model.joblib` artifact is never loaded during evaluation. The reported MAE (28.386 min) and coverage (97.70%) are uncontaminated and genuinely measured on held-out data.
  - **Graph Audit**: Verified that `_graph_stage()` explicitly calls `detect_conflicts()`. The `graph_adjustment_rows: 0` backtest result is strictly a data availability limitation (the journey-level historical dataset lacks concurrent station-pair state) and not a missing function call.

- **2026-09-11**: README.md fully rewritten. Removed corrupted UTF-16LE tail (bytes 6504-85579, entire PAGE_ARCHITECTURE.md garbage). Added: SIH context, PS 26028 reference, existing-system comparison table, Inherits/Faces/Translates explanation, hard vs soft conflict framing, five-stakeholder output table, evaluation methodology note, repo structure map, additional known-limitations bullets (scalability_benchmark bug, FEATURE_STATUS number discrepancy, stub features list, ADWIN Phase 2 status).

- **2026-09-11**: Winning-project improvement pass — senior code review findings addressed (Items 1-7).
  - SHAP explanations (already computed in `conformal.py`'s `predict()` but previously dropped before the API response) are now surfaced through `/predict/{train_id}` via `shap_explanation` and `shap_text` fields added to `PredictionResponse`. New test `test_prediction_includes_shap_explanation` added and passing; `docs/DEMO_SCRIPT.md` now has a "Why does it predict that?" presenter section.
  - Fixed `jobs/scalability_benchmark.py`: was silently broken (`p.model` doesn't exist on `RippleETAPipeline`, `AttributeError` swallowed by a bare `except: pass`, reporting near-zero latency doing nothing). Added a public `RippleETAPipeline.predict_features()` method and rewired the benchmark to use it, sampling from the already feature-engineered dataset (`p._data`) instead of re-reading the raw un-engineered parquet, and replaced the bare except with `(KeyError, ValueError)` plus a success/failure count in the output. Could not execute it in this environment — `data/` does not exist here (gitignored, matches README's expected local-setup step) — so no real latency number is reported; run `python -m jobs.scalability_benchmark` locally with real data to get one. `eval/bench_propagation.py`'s 500-trains/20.58ms remains the cited scale claim.
  - Added a fourth baseline model to `eval/baseline_comparison.py`: an XGBoost regressor trained on the same chronological split but with `prior_leg_delay` (the only cross-journey/network signal) removed, isolating whether network-aware feature engineering earns its complexity versus a plain per-journey regression. Could not run it here (same missing-`data/` constraint) — no numbers added to `docs/RESULTS.md`; run `python eval/baseline_comparison.py` locally to get the fourth row.
  - Reconciled the coverage/MAE contradiction between `docs/FEATURE_STATUS.md` (was 89.9%/27.95 min/81.7 min) and `docs/RESULTS.md` (canonical: 97.70%/28.386 min/106.589 min) — `FEATURE_STATUS.md` §3 now leads with the canonical per-route backtest figures and keeps the old all-data figures in a clearly labeled "superseded" block rather than deleting them. Also found and fixed one more stale reference to 89.9% in `dashboard/app.js`'s passenger FAQ copy (the only other repo-wide hit besides `docs/FEATURE_STATUS.md`, per `grep -r "89.9"`/`grep -r "27.95"`).
  - Widened CI lint/type checks in `.github/workflows/tests.yml` from 2 files to also cover `src/graph/`, `src/calibration/`, `src/api/`, `src/features/`, `src/pipeline.py`. Discovered first that an unpinned `pip install ruff` resolves a much larger rule surface on this ruff version than the classic E4/E7/E9/F baseline — even the previously-covered 2 files failed under it — so added `pyproject.toml` pinning `select = ["E4","E7","E9","F"]`, and pinned exact `ruff==0.16.7`/`mypy==2.3.1` versions in the CI install step so lint scope no longer drifts with whatever is newest at CI run time. Fixed all real findings this surfaced in the newly-covered paths (dead code, unused imports/variables, one-line-if style, a silently-dropped `historical_stations` field on `GraphDemoResponse` that pydantic was discarding, several `Optional`/`Literal` type-narrowing gaps) — verified the exact CI commands pass locally before committing to the workflow change.
  - Added Mondrian (stratified) conformal calibration to `src/calibration/conformal.py`: an optional `use_mondrian` flag buckets the calibration set by `prior_leg_delay` magnitude (0-15/15-60/60+ min, falling back to the pooled calibrator for any bucket under 20 calibration rows) and fits a separate `SplitConformalRegressor` per bucket. Added `per_bucket_coverage()` helper and wired it into `eval/comprehensive_evaluation.py` (STEP 3D: pooled vs. Mondrian coverage side by side, per bucket) and into `train_and_calibrate()`'s returned metrics when enabled. Added `test_mondrian_calibration_holds_per_bucket_coverage_floor` in `tests/test_calibration.py` on synthetic heteroscedastic data — passing, all populated buckets ≥75% coverage. Off by default (`use_mondrian: false` unless set in `config.yaml`'s `model` section) — enabling it on the real dataset and reporting real per-bucket numbers is follow-up work for whoever has the data locally.
  - Relabeled drift-detection language in `docs/ARCHITECTURE_AND_INTELLIGENCE.md` and `docs/LIMITATIONS.md`: searched every doc for "ADWIN" (`docs/`, `README.md`, `docs/research/*`) — none currently overstate it as implemented, all already frame it as Phase 2. Improved precision instead: both docs now explicitly credit the real, working mechanism (`jobs/nightly_recalibration.py`'s 15-minute static MAE threshold on a frozen chronological holdout, gated by the fail-closed `evaluate_promotion()`) rather than only describing what's absent, while still being clear it is a static threshold, not ADWIN/Page-Hinkley change-point detection.
  - `ROUND2_READINESS.md` referenced by this prompt does not exist anywhere in the repo (root or `docs/`) — could not mark items D/E/N `[x]` in it. Flagged to the user; not fabricated.
  - Full test suite after this pass: **40 passed, 1 skipped** (`tests/test_backtest_harness.py` skips without local data, same as baseline before this pass).

- **2026-09-11**: Verification pass + remaining Round 2 items (Tasks 0-7). Working solo this session, committing locally throughout (7 commits, no push). Full test suite went from 40 passed/1 skipped to **52 passed, 1 skipped**.
  - **Task 0**: Regenerated `ROUND2_READINESS.md` at the repo root (confirmed genuinely missing, not fabricated last session either) and updated it incrementally through every task below — every `[x]` reflects an actual command run and observed result, not code-presence assumption.
  - **Task 1 — verification, not assumption**: Ran every test file the prompt listed and recorded real results: `test_midnight_crossing.py` (1 passed), `test_dedup_watermark.py` (1 passed), `test_schema_validation.py`+`test_validation.py` (3 passed), `test_graph.py`+`test_graph_boundaries.py` (12 passed), `test_invariants.py` (3 passed), `test_golden_scenarios.py` (3 passed), `test_backtest_harness.py` (SKIPPED — confirmed reason: "checked-in historical artifact is optional locally," genuinely blocked on missing `data/`, not failing). For Item K (incremental propagation), wrote a probe script instead of trusting code presence and **found a real bug**: `CachedPropagationEngine.propagate_incremental()` derived its recompute boundary from `changed_nodes` alone, which is unsafe — reproduced with two independent trains where naming an unrelated later-sorting node as "changed" caused the function to silently return 0.0 for an active pinned delay's own downstream cascade instead of the correct propagated value. Fixed by deriving the boundary from the full `delays` set instead (provably safe by topological-order argument). Confirmed the fix by reverting it via `git stash` and watching the new regression test fail, then restoring it and watching it pass. Not used anywhere in the live serving path (only `eval/bench_propagation.py`'s benchmark), so no prediction was ever affected by this bug.
  - **Task 2**: Added `segment_metrics()` to `eval/baseline_comparison.py`, segmenting all 4 baseline-comparison models by delay magnitude (0-15/15-60/60+ min), forecast horizon (<4/4-12/>12 hrs), and route (documented zone proxy — no true IR-zone identifier exists in the public dataset). 4 synthetic tests in new `tests/test_segmented_evaluation.py` prove the segmentation logic itself is correct. Real per-segment numbers remain blocked on the missing dataset.
  - **Task 3**: Confirmed `engineer_all_features()` is the single shared feature implementation with no duplicated logic anywhere (grepped every call site). Proved the real, exercised serving path (`RippleETAPipeline.run()` slicing an already-batch-engineered row) is byte-identical to the training path. While proving this, **found a genuine latent skew risk**: `CalibratedPredictionPipeline.predict()`'s fallback re-engineering branch would silently compute `prior_leg_delay` as 0 instead of the true value if ever called with an isolated single row with no per-train history (confirmed empirically — not reachable by any current caller today, but a real landmine for a future one). Added a runtime warning log and a `docs/LIMITATIONS.md` entry rather than leaving it silent.
  - **Task 4**: Scanned full git history (`git log -p --all`) for credential patterns and specifically `.env`/`*railradar*`/`*config*` file history. No real third-party credential was ever committed — RailRadar/NTES references in history are all clearly-labeled placeholders or environment-sourced, and only `.env.example` (never a real `.env`) was ever tracked. **Found one real, currently-live weakness**: `src/api/auth.py` fell back to a hardcoded JWT signing secret (`'super-secret-rippleeta-key'`) whenever `JWT_SECRET` was unset — committed to git, readable by anyone with repo access, usable to forge session tokens. Fixed to generate a fresh random secret per process start instead when unset; `.env.example` now documents setting a real one for production. No external provider to rotate — the code fix is the mitigation.
  - **Task 5**: Both graceful-degradation mechanisms (ML-failure persistence fallback, stale-feed interval widening) already existed in `src/api/app.py` but had **zero real test coverage** — `tests/test_api_fallback.py` was a manual print-script with no asserts that trivially "passed" regardless of behavior. Writing real tests for it **surfaced a genuine bug**: the `/predict/{train_id}` endpoint unconditionally discarded the correct reason message from `get_prediction()` (e.g. "ML unavailable. Using persistence baseline.") and replaced it with a generic "Calibrated network-aware prediction." string based only on `anomaly_flag` — so a degraded or stale-widened response looked fully confident instead of flagging why. Fixed to preserve the real message. Also added the missing `stale_since` timestamp (was computed nowhere) to both the response logic and the `PredictionResponse` model. 3 new real tests in `tests/test_api_fallback.py` prove both fallback paths engage correctly and the response is never null in either failure mode.
  - **Task 6**: Confirmed the exact gap the prompt asked about: `src/pipeline.py` computes real provenance (git commit, dataset/model SHA-256) per prediction, but it was only ever returned in the API response and discarded — the existing `prediction_logs` SQLite table recorded a hardcoded `"rippleeta-v1.0.0"` placeholder instead. Added `input_features` to `RippleETAPipeline.run()`'s result and a new `prediction_audit_log` SQLite table (existing `prediction_logs` left untouched, still used by `/api/stats`) persisting train_id, full output, real provenance, the input feature vector (JSON), and the SHAP explanation text. 2 new tests in `tests/test_audit_log.py` query the SQLite file directly and prove a real, recoverable row lands there for both healthy and degraded predictions.
  - **Task 7** (low-priority, time-boxed as instructed): Grepped `dashboard/` and `frontend/` directly for any low-bandwidth/2G fallback — none exists. Documented as an acknowledged, unaddressed gap in `docs/LIMITATIONS.md`; did not attempt to build one.
  - **Still blocked on the missing dataset in this environment** (`data/` does not exist here): Item D's 4th baseline model has no real numbers yet; Item E's segmentation has no real per-segment numbers yet; `test_backtest_harness.py` still skips; Mondrian conformal (Item F, implemented last session) still has no real per-bucket numbers on the actual data.
  - **Not addressed this session** (out of scope of Tasks 0-7, left honestly unchecked in `ROUND2_READINESS.md`): Live/Replay dashboard toggle, a recorded RailRadar/NTES capture for offline demo, one full stakeholder dashboard verified end-to-end with live data, Item H (point-in-time backfill audit), Item L (verifying `make seed` loads a real recorded capture vs. a stub).
  - Full test suite after this session: **52 passed, 1 skipped**.

- **2026-09-11**: Verification pass 2 — dataset blocker resolution attempt, full test-suite audit, JWT follow-up, remaining Round 2 items. Working solo, no push/pull this session either. Full test suite went from 52 passed/1 skipped to **65 passed, 1 skipped**. 12 commits.
  - **TASK 0 — dataset blocker: STILL UNRESOLVED, stated plainly.** This has now blocked four consecutive sessions. Confirmed the exact requirement from `README.md`/`src/ingestion/load_kaggle.py`: a raw CSV at `data/raw/train_delay.csv` with columns `train_number`, `journey_date`, `actual_delay_minutes`, `scheduled_travel_hours`, `distance_km`, `zone_congestion_index`, `monsoon_flag`, `fog_risk`, `coach_count`, `loco_age_years` (≥100 rows per `config.yaml`'s `min_rows`; the original canonical-numbers run used 10,000 rows / 56 train numbers). Ran an exhaustive search (`find . -iname "*.csv" -o -iname "*.parquet"`, plus parent directories and Downloads) — genuinely nothing exists anywhere in this environment; only unrelated library test fixtures under `.venv` matched. Also found and fixed a real documentation bug while investigating: `README.md` falsely claimed "the checked-in raw CSV is sufficient for the demo" directly contradicting its own note two paragraphs above that `data/` is gitignored — confirmed via `git log --all --diff-filter=A` that no CSV/parquet has ever been committed to this repo. Corrected the README and added the exact required-column list. No synthetic data was generated or presented as real; all downstream real-number-dependent items remain honestly blocked (see below).
  - **TASK 1 — full test-suite audit.** Went through all 17 `tests/*.py` files (AST scan for `assert`/`pytest.raises` per test function, plus manual read of every file) looking for the `test_api_fallback.py` pattern (prints instead of asserts, trivially passes regardless of behavior) elsewhere. **Found one more**: `tests/test_schema_validation.py` used `try/except ValidationError` blocks that only printed "SUCCESS"/"FAILED" and never asserted or raised — it would pass whether or not `LiveStatusSchema`/`RouteSchema` validation actually worked. Verified the underlying validation logic was genuinely correct in all 4 original cases before rewriting (a test-quality bug, not a code-correctness bug) — replaced with 6 real `pytest.raises(ValidationError)` tests. Every other test file was confirmed to have real assertions; `test_backtest_harness.py` already uses the correct `@pytest.mark.skipif(reason=...)` pattern for an environment-gated test.
  - **TASK 2 — JWT secret follow-up.** Traced the actual auth flow in `src/api/auth.py`: confirmed RippleETA issues its own 12-hour session JWT (`create_session_token`, stored in the `rippleeta_session` cookie) and validates *that* on every request — `verify_google_token` is only used once at initial login. So the concern (random-per-restart dropping live sessions) was real. Confirmed the existing fix (`os.environ.get('JWT_SECRET') or secrets.token_hex(32)`) already does the right thing — reads the env var first, random fallback only when unset — and added 4 tests in `tests/test_jwt_secret_behavior.py` proving it concretely (stable across simulated restarts when set; the documented unconfigured-restart tradeoff pinned so it can't regress into something worse). Confirmed via `grep -rn "super-secret-rippleeta-key"` that no reference to the old value remains in tracked source (only historical mentions in this file and `ROUND2_READINESS.md`, plus one stale untracked `__pycache__` file which was deleted).
  - **TASK 3.1 — Live/Replay toggle.** Checked before building: it already existed. `dashboard/app.js`'s `updateMode()` reads `/system/status` and drives a real, visible `.ticker-mode` badge ("LIVE FEED"/green vs "REPLAY"/amber, genuinely distinct CSS) present in every stakeholder view. Verified the backend contract matches what the frontend expects via a direct API smoke test, and pinned it with 2 new tests in `tests/test_api.py`. Since no `RIPPLEETA_API_KEY` has ever been available, only the REPLAY state has actually been observed — the LIVE path is real but unexercised.
  - **TASK 3.2 — recorded RailRadar/NTES capture.** Time-boxed to a few minutes as instructed. Confirmed `data/replay/` doesn't exist anywhere. Genuinely requires a live multi-hour data-collection session; not attempted.
  - **TASK 3 — Item H (point-in-time backfill audit).** Audited `src/features/engineering.py`: `rake_delay_inheritance()` is the only genuinely temporal computation, and it sorts by `[train_number, journey_date]` internally before `shift(1)`, so it's correct regardless of input order. Proved this concretely with 2 new tests: one plants a sentinel delay (999.0) on a train's latest journey and confirms no earlier row's `prior_leg_delay` ever leaks it — explicitly constructing the naive `shift(-1)` alternative alongside it to prove that version *would* leak, so the test is verifiably meaningful. The other feeds records in scrambled row order and confirms identical output. The chronological train/cal/test split was reviewed directly (simple sort+slice arithmetic, no leakage path) rather than given a low-value runtime test.
  - **TASK 3 — Item L (offline seed).** `make` isn't installed in this environment, so ran `python scripts/seed_db.py` directly (exactly what the Makefile target does) instead of assuming. Result: `ERROR:__main__:Replay directory data\replay not found.` Read the script in full — it's genuine, correct logic that parses real capture JSON into SQLite, not a stub that fabricates fake data. Confirmed this is the same underlying gap as the RailRadar capture item, not a separate one.
  - **Still blocked on the missing dataset**, unchanged from before: Item D's 4th baseline model, Item E's real per-segment numbers, Item F's (Mondrian) real per-bucket numbers, `test_backtest_harness.py`, Item L / the RailRadar capture.
  - **Not attempted this session** (out of scope of the numbered tasks): one full stakeholder dashboard verified end-to-end with live/replay data.
  - Full test suite after this session: **65 passed, 1 skipped**.

- **2026-09-11**: Dataset-arrival check (attempt 5) — still not present; light cleanup only.
  - **TASK 0: the dataset has NOT arrived.** Checked `data/raw/train_delay.csv` directly (doesn't exist; `data/` doesn't exist at all) and re-ran the exhaustive search from prior sessions plus a wider sweep of nearby project directories and `Downloads/` — nothing matching the train-delay schema turned up anywhere (only files from an unrelated "Unihack" project and a couple of unrelated stray CSVs). This is the same blocker as the previous four sessions, not a schema mismatch — Tasks 1 and 2 (real-number runs, `docs/RESULTS.md`/`ROUND2_READINESS.md` updates) were skipped entirely rather than fabricating or forcing a run. No numbers were produced or altered this session.
  - **TASK 3 (data-independent cleanup) — done.** Searched for stale "checked in"/"is included"/"ships with" claims (`grep -rn` across `docs/` and `README.md`, plus a broader sweep for similar phrasing like "bundled"/"comes with"/"pre-loaded") — found only the already-corrected text from the previous session's README fix; nothing new or stale. Re-ran the full suite (**65 passed, 1 skipped**, unchanged) and did a rough assert-count-vs-test-count sanity pass across every `tests/*.py` file (`grep -c` for `assert`/`pytest.raises`/etc. per file) — every file clears a healthy ratio (lowest was `test_invariants.py` and `test_validation.py` at 1:1; manually confirmed `test_invariants.py`'s 1:1 is legitimate since it's Hypothesis property tests, each assertion exercised against ~100 generated inputs per run, not weak). No further fake tests found beyond the two already caught in prior sessions.

- **2026-09-11 (Complete codebase audit — security, cleanliness, connection map, design system, visual backlog)**: Solo session (picked up mid-flight from another device's session, which had already completed Part 0 scope inventory and Part 1 backend security before going offline — this session independently corroborated Part 1's findings via a separate fork, then continued from Part 2 onward). No push/pull. 8 checkpoint commits. Full results: `docs/AUDIT_REPORT.md` (structured Parts 0-6) and `docs/CONNECTION_MAP.md` (the complete element-by-element frontend-backend table for all 7 stakeholder pages, superseding informal status claims in `docs/FEATURE_STATUS.md`/`docs/PAGE_ARCHITECTURE.md`/`ROUND2_READINESS.md` wherever they conflict).
  - **Security (Parts 1-2):** Backend clean on `pip-audit`/CORS/SQL-injection/secrets; confirmed the JWT-secret fix and audit-log persistence fix both still correct. Real gaps found: no rate limiting on any endpoint (Important, undisclosed prior to this pass); `google_login()` leaks raw exception text on failed auth (Minor). Frontend: no hardcoded secrets, Google OAuth client-side scope correct, zero npm dependency surface. Found and documented that `frontend/js/api.js`'s `signOut()` calls a `POST /api/auth/logout` route that **does not exist** in `src/api/app.py` — the httponly session cookie is never invalidated server-side, so "Sign Out" doesn't actually end a session (Important, not fixed — touches the backend route table beyond a trivial fix). Also flagged inconsistent `innerHTML` escaping discipline across ~15 sites in `dashboard/app.js`/`frontend/js/dashboard.js` (Minor, not demonstrated exploitable).
  - **Cleanliness (Part 3):** Removed 26 confirmed-dead one-off `patch_*.py`/`fix_*.py` scripts at the repo root. Fixed everything `ruff` flagged as real dead code. While fixing 7 `E402` findings, made and then caught-and-corrected an inaccurate claim in the same session's own commit message (moved 3 genuinely-misplaced imports, not "deliberately lazy" as first asserted without individually checking each one — the correction commit says so explicitly). Ran `black` across the whole Python backend as an isolated formatting commit (never run before this session — 47 of 54 files reformatted, zero logic change). While gauging `prettier`'s scope with `--check`, it refused to parse `dashboard/styles.css` and `frontend/index.html` with hard syntax errors rather than style warnings — found and fixed 3 corrupted CSS declarations (leftover fragments fused onto valid rules) and one unbalanced `<div>` missing its opening tag in the navbar (the matching close tag and the CSS rule for it both already existed, confirming a lost tag, not deliberate unwrapping). Added code comments pinning REAL/PARTIAL/HARDCODED status directly above the specific fields `docs/LIMITATIONS.md` describes as proxy/stub (`ripple_score`, `financial_impact_inr`, `weather_risk_flag` and friends) so the status survives future edits without needing the external doc. Flagged (not silently fixed) a duplicated-logic instance: the COMMIT/DEFER `≤30min` threshold rule is independently reimplemented in `dashboard/app.js`'s junction table instead of reading the backend's authoritative `platform_commit` field.
  - **Connection map (Part 4 — the centerpiece):** Built via 3 parallel forks (2 pages each) plus 2 pages done directly, covering all 7 stakeholder views + global elements. Answered the audit's specific cross-reference questions with evidence: `shap_explanation`/`shap_text` are genuinely computed and returned by the API but **never consumed anywhere in the frontend** (only static bibliography citation text exists). Neither `degraded` nor `stale_since` is ever read in `dashboard/app.js` — a degraded/stale response is visually indistinguishable from normal on 5 of 7 pages; Crew Controller and Feeder Transport actively discard their own endpoint's real `message` field and replace it with a client template on every branch. No response-model field-shape mismatches found on any page. New findings beyond anything previously claimed fixed: Crew Controller's entire dominant decision badge is driven by a **fabricated** HOER duty-elapsed formula with no real crew/CMS data behind it (the single most misleading element found this session); Control Room's `/graph/demo` is called with **zero parameters**, so the network diagram never changes regardless of the selected train; Ghost Sandbox's documented offline-fallback capability lives only in an orphaned `dashboard/sandbox.js` that **no HTML file actually loads**; both "Network Ripple Score" gauges (Station Master, Control Room) are flat hardcoded `55`s with zero live computation.
  - **Design system conflict (Part 5 — reported, not resolved):** Found **five** distinct, non-interoperable color systems currently coexisting (not the two originally suspected): the documented light cream/indigo/marigold `DESIGN_SYSTEM.md` (implemented nowhere), `dashboard/styles.css`'s actual dark-cinematic palette (whose own header comment claims fidelity to `DESIGN_SYSTEM.md` while visibly contradicting it), a third dark ink/teal/amber system documented in `FRONTEND_PAGES_FEATURES.md`/`PAGE_ARCHITECTURE.md` matching neither, `frontend/style.css`'s independent fourth dark palette (the newly-arrived teammate frontend), and `eta/`'s fifth, light rail-blue/yellow palette. One concrete on-page bug regardless of which system wins: Station Master's SVG signal lamp is hardcoded to `DESIGN_SYSTEM.md`'s hex values while the rest of that page uses `dashboard/styles.css`'s different actual tokens. Also confirmed `docs/PAGE_ARCHITECTURE.md` is substantially stale (still describes a tab-based single-page `dashboard/index.html` and "OAuth: zero code written," both false against current code).
  - **Prior-claim verification (Part 4 of the report):** Could not find any evidence in `git log --all` or any doc that the "reconciliation audit," "architecture-doc verification pass," or "8 screenshot-grounded UI bugs" referenced in this audit's own brief ever actually happened in this repo's history — stated plainly in `docs/AUDIT_REPORT.md` rather than fabricated. `docs/PAGE_ARCHITECTURE.md`'s staleness (above) is exactly what such a pass would have caught.
  - **Visual backlog (Part 6):** Kept as its own file, `docs/VISUAL_BACKLOG.md` — prioritized, with several items honestly marked UNVERIFIED (contrast ratios, mobile viewport, loading-state affordances) since this session had no browser/dev-server access to confirm them visually, rather than guessing a score.
  - **Not done this session, explicitly deferred**: `ROUND2_READINESS.md` was intentionally NOT updated with new checklist items from this audit's findings, per the audit's own instructions — waiting for the user to review both reports and decide what's in scope before that checklist changes.

- **2026-09-11 (Critical demo-safety fixes, post-audit)**: Solo session, no push/pull, 11 checkpoint commits (`c003a6a` through `aef6145`). Worked through the highest-severity findings from the prior audit session in the order given, re-running the full test suite after every change (68/68 passed throughout, ruff/black/prettier clean throughout). No browser/dev-server was available this session either — every fix's commit message documents the strongest verification actually performed (Node simulations of the pure-function logic, real FastAPI `TestClient` calls against the live backend, cross-checks against the real computation a fallback depends on) and states plainly what manual browser check is still owed, rather than claiming visual confirmation that didn't happen.
  - **Task 1 — Crew Controller decision badge: reported, not implemented, per instructions.** Read the actual fabricated `elapsedMinutes` formula (`dashboard/app.js:611`) and traced how deeply it's entangled — it drives not just the primary badge but a whole duty-timeline visualization, the risk-pill matrix, and the sign-on/expiry clocks, independently re-fabricated a second time in the multi-train dispatch board. Corrected the task's own premise along the way: Ripple Score/INR are NOT actually disclosed as illustrative anywhere in the live UI (checked `station-master.html` directly) — the real disclosure precedent is the "REPLAY / SIMULATION SNAPSHOT" tag elsewhere on that page. Reported Option A (promote the real `relief_dispatch_deadline` field — real estimate 2-4 hours, closer to a partial redesign than a variable swap, since the real field isn't actually HOER-hours math either) and Option B (disclose clearly — real estimate 20-30 min) with honest effort estimates. Stopped without implementing either, awaiting a decision.
  - **Task 2 — Multi-train table silent row-drop, fixed.** All 5 tables (not 4 — Control Room's regional matrix was a fifth instance the backlog item undercounted), not just the 4 the backlog item named. Added a shared `failedRowHtml()` helper; also fixed a `.then().catch()` ordering bug in the Station Master junction table that lost the failed train's ID entirely. Verified via an isolated Node simulation. Commit `c003a6a`.
  - **Task 3 — Ghost Sandbox offline fallback, fixed.** Investigated wiring in the existing `sandbox.js` first, per the task's stated first option, and found it genuinely stale (wrong element IDs for the current `sandbox.html`, would have added a second conflicting slider listener rather than working). Built a real fallback in `app.js` instead and removed the dead file. Independently re-verified the ported max-plus formula against the live `src/graph/sandbox_endpoint.py` for 4 delay values — byte-identical output. One process note: the first attempt at this commit (`058e493`) had its `git add` fail atomically on a since-deleted path, silently dropping the actual code changes from the commit despite an accurate message describing them — caught by checking `git status` immediately after committing (now a standing habit for the rest of this session) and fixed in a follow-up commit (`c5ab7e3`) with an honest note about what happened.
  - **Task 4 — Control Room graph parameterization, fixed with an honestly reported scope limit.** The underlying timed-event graph has real schedule data for exactly two trains (12301, 56789) on one fixed corridor — a genuinely different diagram per arbitrary selected train would require building real timetable data for every other train, which is out of this session's scope and was reported as such rather than faked. What's real: `/graph/demo` now accepts `train_id`, and honestly discloses via a new response field (and a visible UI banner) when the selected train has no network data, instead of silently rendering the fixed scenario as if it applied. Verified end-to-end with real `TestClient` calls for 4 different train IDs. Commit `8ed349d`.
  - **Task 5 — SHAP and degraded/stale wired into the UI, fixed.** Added shared `renderShapText()`/`renderDegradedStaleBadge()` helpers; wired SHAP + degraded/stale into Passenger and Station Master, degraded/stale only into Crew Controller (per the task's "one or two pages is enough" allowance for SHAP). Verified against 3 real backend states via `TestClient`: a genuine non-trivial SHAP response, a forced `degraded:true` response (reusing the existing `BrokenPredictionService` test double), and a forced stale-feed response (reusing the existing `fake_get_live_status` test setup) — all three render correctly. Commit `e3416a5`.
  - **Task 6 — Signal lamp color fix, fixed.** Replaced all 9 hardcoded hex fill values in Station Master's SVG signal lamp with `var(--success|--signal|--stamp)` plus an opacity toggle for lit/unlit, so it now tracks whatever the still-open design-system decision eventually resolves to instead of needing a second manual fix later. Commit `4e37e18`.
  - **After all tasks**: updated `PROGRESS.md` (this entry), `ROUND2_READINESS.md` (SHAP checklist item + a new dated summary section), and `docs/VISUAL_BACKLOG.md` (items 1/2/4 marked fixed with commit references, item 3 explicitly left untouched). Full test suite re-run clean.
  - **Explicitly not touched, per instructions**: the five-way design-system conflict (Part 5 of the audit) — still the user's decision. Crew Controller's fabricated decision badge — reported, awaiting a choice between the two options above.

